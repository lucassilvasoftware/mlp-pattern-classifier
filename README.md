# RNA — classificador de padrões

Repositório do **laboratório 6** de **Inteligência Artificial**: rede neural artificial (RNA) aplicada à **classificação de padrões**. O foco típico do experimento é treinar e avaliar um modelo sobre um conjunto de dados rotulado e analisar métricas de desempenho.

## Tecnologias

- Conforme o material do curso (por exemplo Python com NumPy/TensorFlow/PyTorch — adicione os arquivos de código e `requirements.txt` ao versionar o trabalho completo).

## Como executar

1. Quando os scripts e dependências estiverem no repositório, crie um ambiente virtual e instale as bibliotecas indicadas pelo professor ou por um arquivo `requirements.txt`.

2. Execute o notebook ou o script principal de treino/avaliação fornecido no enunciado do laboratório.

Se esta cópia local ainda não contiver os fontes, sincronize com o branch ou o repositório onde o notebook e os dados foram publicados.
